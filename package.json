{
  "name": "rblx",
  "version": "1.5.1",
  "description": "A Roblox promise-based library for NodeJS with support for Open Cloud and the Web APIs",
  "main": "dist/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tsc && mocha",
    "build": "tsc && npm pack --pack-destination=build"
  },
  "keywords": [
    "roblox",
	"open cloud",
	"opencloud"
  ],
  "author": "RyloRiz",
  "license": "ISC",
  "files": [
    "/dist"
  ],
  "type": "commonjs",
  "dependencies": {
    "assert": "^2.0.0",
    "axios": "^0.26.1",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "dotenv": "^16.0.0",
    "mocha": "^9.2.2"
  }
}
